name: Build and Test

on:
  push:
    paths-ignore:
      - "docs/**"
      - "readme.md"
      - "**/.rst"
      - "**/.jpg"
      - "**/.png"
      - "**/.md"
      - "**/.yml"
    branches: [ master ]
  pull_request:
    paths-ignore:
      - "docs/**"
      - "readme.md"
      - "**/.rst"
      - "**/.jpg"
      - "**/.png"
      - "**/.md"
      - "**/.yml"

jobs:
  build:
    name: build and test
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.8]
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        pip install build>=0.10.0
        pip install .[dev]
        python -m build
    - name: Run Linting
      run: |
        black .
    - name: Run Tests
      run: |
        python -m pytest --continue-on-collection-errors tests -v


